import React from 'react';
import Courses from './courses';

// Root component of the app
const CoreComponentsApp = () => {
    // Render courses component as the main content
  return (
    <Courses />
  );
};

export default CoreComponentsApp;